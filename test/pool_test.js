'use strict';

module.exports = {
    'query Tests' : {
        'it should': function (test) {
            //
            test.expect(1);
            test.done();
        }
    },
    'transaction Tests' : {
        'it should': function (test) {
            //
            test.expect(1);
            test.done();
        }
    },
    '_processOnSingleConnection Tests' : {
        'it should': function (test) {
            //
            test.expect(1);
            test.done();
        }
    }
};